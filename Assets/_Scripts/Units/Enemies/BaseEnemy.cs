using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BaseEnemy : BaseUnit
{
    // Start is called before the first frame update
    [SerializeField] public bool miniMax;
}
